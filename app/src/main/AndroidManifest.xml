<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.emargystudio.bohemeav0021">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/bohemea"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Cinema.MovieDetailsActivity"/>
        <activity android:name=".Cinema.CinemaCategoryActivity" />
        <activity android:name=".History.HistoryActivity" />
        <activity android:name=".Menu.FoodDetailActivity" />
        <activity android:name=".Profile.ProfileActivity" />
        <activity android:name=".Cart.CartActivity" />
        <activity android:name=".Cinema.CinemaActivity" />
        <activity android:name=".Menu.MenuActivity" />
        <activity android:name=".ReservationMaker.ReservationActivity" />


        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <provider
            android:name="com.facebook.FacebookContentProvider"
            android:authorities="com.facebook.app.FacebookContentProvider327368101452898"
            android:exported="true" />

        <activity
            android:name=".HomeActivity"
            android:label="Bohemea art cafe">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>